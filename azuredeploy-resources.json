{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "locations": {
      "type": "array"
    }
  },
  "variables": {
    "uniqueString": "[uniqueString(resourceGroup().Id)]"
  },
  "resources": [
    {
      "type": "Microsoft.Resources/deployments",
      "apiVersion": "2021-04-01",
      "name": "[concat(parameters('locations')[copyIndex()].location, '_network_', variables('uniqueString'))]",
      "resourceGroup": "[concat(parameters('locations')[copyIndex()].location, '_shared')]",
      "properties": {
        "mode": "Incremental",
        "expressionEvaluationOptions": {
          "scope": "inner"
        },
        "templateLink": {
          "uri": "https://raw.githubusercontent.com/SaileshBellamkonda/azure-arm/main/arm-network.json",
          "contentVersion": "1.0.0.2"
        },
        "parameters": {
          "virtualNetworkInformation": {
            "value": "[parameters('locations')[copyIndex()].virtualNetwork]"
          }
        }
      },
      "copy": {
        "name": "locationName",
        "count": "[length(parameters('locations'))]"
      }
    },
    {
      "type": "Microsoft.Resources/deployments",
      "apiVersion": "2021-04-01",
      "name": "[concat(parameters('locations')[copyIndex()].location, '_storage_', variables('uniqueString'))]",
      "resourceGroup": "[concat(parameters('locations')[copyIndex()].location, '_shared')]",
      "dependsOn": [
        //"[resourceId('Microsoft.Resources/deployments', concat(parameters('locations')[copyIndex()].location, '_network_', variables('uniqueString')))]"
      ],
      "properties": {
        "mode": "Incremental",
        "expressionEvaluationOptions": {
          "scope": "inner"
        },
        "templateLink": {
          "uri": "https://raw.githubusercontent.com/SaileshBellamkonda/azure-arm/main/arm-storage.json",
          "contentVersion": "1.0.0.0"
        },
        "parameters": {
        }
      },
      "copy": {
        "name": "locationName",
        "count": "[length(parameters('locations'))]"
      }
    }
  ],
  "outputs": {}
}
